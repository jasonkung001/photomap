name = "photomap"
pages_build_output_dir = "dist"
compatibility_date = "2024-03-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Note: GEMINI_API_KEY should be set as a secret via `wrangler pages secret put GEMINI_API_KEY`
